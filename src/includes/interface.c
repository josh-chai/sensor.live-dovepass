#include <stdio.h>
#include <string.h>
#include "interface.h"

struct Interface get_interface()
{
    struct Interface interface;
    strcpy(interface.mac_address, "78:31:c1:bd:9b:30");
        printf("%s", "avvvbss");
    return interface;
}